import React from 'react';

const Navbar = () => {
  return (
    <nav className="bg-primary text-primary-foreground h-16 flex items-center px-8">
      <div className="text-xl font-bold">WellStride</div>
      {/* Notification and user avatar can be added here if needed */}
    </nav>
  );
};

export default Navbar; 